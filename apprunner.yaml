version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "No custom build needed - using Dockerfile"
run:
  network:
    port: 5000
    env: PORT
  env:
    - name: vulnerable
      value: "1"
    - name: tokentimetolive
      value: "300"